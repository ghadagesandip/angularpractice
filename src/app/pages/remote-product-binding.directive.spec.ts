import { RemoteProductBindingDirective } from './remote-product-binding.directive';

describe('RemoteProductBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoteProductBindingDirective();
    expect(directive).toBeTruthy();
  });
});
